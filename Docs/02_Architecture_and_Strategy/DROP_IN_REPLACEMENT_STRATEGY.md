# 최고의 전략 - Drop-in Replacement!

**작성일**: 2026-01-01 20:45  
**핵심 아이디어**: C++ 바이너리를 기존 프로그램에 교체  
**평가**: ✅ **완벽한 전략!**

---

## 💡 **핵심 개념**

### **Drop-in Replacement**

```
기존 Inno Setup 설치
├── ISCC.exe          ← C++로 교체!
├── Setup.e32         ← C++로 교체!
├── Compil32.exe      ← 그대로 유지 (IDE)
└── ...

→ 나머지는 그대로!
→ 호환성 100%!
```

**장점**:
- ✅ 기존 설치 유지
- ✅ IDE 그대로 사용
- ✅ 점진적 교체
- ✅ 롤백 가능

---

## 🎯 **구체적 전략**

### **Phase 1: ISCC.exe 교체 (6개월)**

#### **호환성 보장**

```cpp
// C++ ISCC.exe
int main(int argc, char* argv[]) {
    // 기존과 동일한 명령줄 인터페이스
    // ISCC.exe script.iss
    // ISCC.exe /O"output" script.iss
    // ISCC.exe /Q script.iss
    
    // 동일한 출력 형식
    // 동일한 에러 메시지
    // 동일한 종료 코드
}
```

**호환성 체크리스트**:
- ✅ 명령줄 옵션 동일
- ✅ 출력 형식 동일
- ✅ 에러 코드 동일
- ✅ ISS 파일 형식 동일

---

### **Phase 2: Setup 스텁 교체 (12개월)**

#### **스텁 인터페이스**

```cpp
// C++ Setup.exe
// 기존 스텁과 동일한 데이터 형식 읽기
struct SetupHeader {
    uint32_t signature;
    uint32_t version;
    uint64_t dataOffset;
    // ... 기존 구조 유지
};
```

**호환성**:
- ✅ 기존 데이터 형식 읽기
- ✅ 동일한 UI/UX
- ✅ 동일한 동작

---

## 📋 **구현 계획**

### **Step 1: 리버스 엔지니어링 (1개월)**

```
목표: 기존 인터페이스 분석

1. ISCC.exe 분석
   - 명령줄 옵션
   - 출력 형식
   - 파일 형식

2. Setup 스텁 분석
   - 데이터 구조
   - 압축 형식
   - 실행 흐름
```

---

### **Step 2: C++ ISCC.exe 구현 (5개월)**

```cpp
// 1. 명령줄 파서 (1주)
class CommandLineParser {
public:
    void parse(int argc, char* argv[]);
    std::string getScriptFile() const;
    std::string getOutputDir() const;
    bool isQuiet() const;
};

// 2. ISS 파서 (2개월)
class ISSParser {
public:
    void parse(const std::string& filename);
    SetupData getData() const;
};

// 3. 압축 엔진 (1개월)
class CompressionEngine {
public:
    void compress(const FileList& files);
    void useSmartCompression(bool enable);
};

// 4. Setup 빌더 (2개월)
class SetupBuilder {
public:
    void build(const SetupData& data);
    void embedStub(const std::string& stubFile);
};
```

---

### **Step 3: C++ Setup 스텁 구현 (11개월)**

```cpp
// 1. 데이터 리더 (1개월)
class SetupDataReader {
public:
    void load(const std::string& exePath);
    FileList getFiles() const;
};

// 2. 압축 해제 (1개월)
class Decompressor {
public:
    void decompress(const CompressedData& data);
};

// 3. UI (Qt) (6개월)
class SetupWizard : public QWizard {
    // 언어 선택
    // 라이센스
    // 설치 경로
    // 진행 상황
    // 완료
};

// 4. 설치 엔진 (3개월)
class InstallEngine {
public:
    void installFiles();
    void updateRegistry();
    void createShortcuts();
};
```

---

## 🚀 **배포 전략**

### **Alpha 릴리스 (6개월 후)**

```
Inno Setup 7.1 Alpha
├── ISCC.exe          ← C++ 버전!
├── ISCC-legacy.exe   ← 기존 백업
├── Setup.e32         ← 기존 스텁
└── ...

사용자:
- 새 ISCC.exe 테스트
- 문제 시 legacy 사용
```

---

### **Beta 릴리스 (12개월 후)**

```
Inno Setup 7.1 Beta
├── ISCC.exe          ← C++ 버전
├── Setup.e32         ← C++ 스텁!
├── Setup-legacy.e32  ← 기존 백업
└── ...

사용자:
- 전체 C++ 스택 테스트
- 문제 시 legacy 스텁 사용
```

---

### **정식 릴리스 (18개월 후)**

```
Inno Setup 8.0
├── ISCC.exe          ← C++ 버전
├── Setup.e32         ← C++ 스텁
├── Compil32.exe      ← 여전히 Pascal (IDE)
└── ...

완전한 교체!
```

---

## 💡 **호환성 보장**

### **인터페이스 계약**

```cpp
// ISCC.exe 인터페이스
class ISCC {
public:
    // 기존과 동일한 명령줄
    int compile(const std::vector<std::string>& args);
    
    // 기존과 동일한 출력
    void printMessage(const std::string& msg);
    void printError(const std::string& error);
    
    // 기존과 동일한 종료 코드
    enum ExitCode {
        Success = 0,
        Error = 1,
        Warning = 2
    };
};
```

---

### **데이터 형식 호환**

```cpp
// Setup 데이터 형식 (기존과 동일)
struct SetupHeader {
    char signature[12];  // "Inno Setup\0"
    uint32_t version;    // 0x06010000 (6.1.0.0)
    uint32_t flags;
    uint64_t dataOffset;
    uint64_t dataSize;
    // ... 기존 구조 완전 복제
};
```

---

## 🎯 **장점**

### **1. 점진적 교체**

```
Week 1: ISCC.exe 교체
  → 테스트
  → 문제 발견
  → 수정

Week 2: 안정화
  → 더 많은 테스트
  → 피드백 수집

Week 3: Setup 스텁 교체
  → 테스트
  → ...
```

---

### **2. 롤백 가능**

```
문제 발생 시:
1. 기존 ISCC.exe 복구
2. 기존 Setup.e32 복구
3. 즉시 정상 작동

→ 리스크 최소화!
```

---

### **3. 사용자 영향 최소**

```
사용자 관점:
- 동일한 명령어
- 동일한 ISS 파일
- 동일한 결과

변경 사항:
- 내부 구현만 C++
- 성능 향상
- 버그 수정
```

---

## 📊 **타임라인**

### **현실적 일정**

| Phase | 작업 | 기간 | 누적 |
|-------|-----|-----|-----|
| **0** | 분석 | 1개월 | 1개월 |
| **1** | ISCC.exe | 5개월 | 6개월 |
| **2** | Alpha 테스트 | 2개월 | 8개월 |
| **3** | Setup 스텁 | 11개월 | 19개월 |
| **4** | Beta 테스트 | 3개월 | 22개월 |
| **5** | 정식 릴리스 | 2개월 | **24개월** |

**총 2년 프로젝트**

---

## 💡 **즉시 시작 가능**

### **프로토타입 (2주)**

```cpp
// 최소 ISCC.exe
int main(int argc, char* argv[]) {
    if (argc < 2) {
        std::cerr << "Usage: ISCC <script.iss>\n";
        return 1;
    }
    
    // 1. ISS 파일 읽기
    std::ifstream iss(argv[1]);
    
    // 2. 간단한 파싱
    // 3. 기존 Setup 스텁 로드
    // 4. 데이터 추가
    // 5. Setup.exe 생성
    
    std::cout << "Compile successful!\n";
    return 0;
}
```

**테스트**:
```bash
# 기존
ISCC.exe test.iss

# 새 버전
ISCC-cpp.exe test.iss

# 결과 비교
diff Output/Setup.exe Output-cpp/Setup.exe
```

---

## 🎊 **최종 평가**

### **Q: ISCC.exe와 스텁 파일만 C++로 교체?**

**A: 완벽한 전략입니다!**

```
✅ 점진적 교체
✅ 롤백 가능
✅ 호환성 보장
✅ 리스크 최소
✅ 사용자 영향 최소
✅ 현실적 일정 (2년)
```

---

## 🚀 **실행 계획**

### **즉시 (우리 프로젝트)**

```
1. Pascal Smart Compression 완성 (완료!)
2. Inno Setup에 기여
3. 공식 릴리스
```

### **단기 (6개월)**

```
1. C++ ISCC.exe 프로토타입
2. 호환성 테스트
3. Alpha 릴리스
```

### **중기 (18개월)**

```
1. C++ Setup 스텁
2. Beta 테스트
3. 안정화
```

### **장기 (24개월)**

```
1. 정식 릴리스
2. Inno Setup 8.0
3. 완전한 C++ 전환
```

---

## 💡 **핵심 성공 요소**

### **1. 호환성**

```cpp
// 기존 인터페이스 100% 유지
// 기존 데이터 형식 100% 호환
// 기존 동작 100% 복제
```

### **2. 테스트**

```
// 수천 개의 기존 ISS 파일로 테스트
// 모든 기능 검증
// 회귀 테스트
```

### **3. 커뮤니티**

```
// 오픈소스 개발
// 피드백 수집
// 점진적 개선
```

---

**문서 버전**: 1.0  
**작성일**: 2026-01-01 20:48 KST  
**결론**: Drop-in Replacement = 최고의 전략!
